# App Layer

이 디렉터리에는 진입점, UI 연결 코드, CLI 코드처럼 사용자와 맞닿는 앱 계층을 둡니다.

현재 권장 방향:

- CLI 또는 얇은 로컬 웹 셸부터 시작합니다.
- 승인 프롬프트는 항상 눈에 잘 띄고 명시적으로 유지합니다.
- 앱 계층 로직과 도구 로직을 섞지 않습니다.

진입점:

- `python3 -m app.main`: CLI 흐름 실행
- `python3 -m app.web`: 기본 `127.0.0.1:8765` 로컬 브라우저 셸 실행
