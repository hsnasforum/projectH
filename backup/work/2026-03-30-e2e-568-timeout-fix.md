# 2026-03-30 E2E 568 timeout fix

## 변경 파일
- `e2e/tests/web-smoke.spec.mjs`

## 사용 skill
- 없음

## 변경 이유
- `verify/3/30/2026-03-30-e2e-568-timeout-not-ready.md`에서 "same-session recurrence aggregate" E2E 시나리오가 60초 timeout으로 간헐 실패한다고 보고되었습니다.
- 원인 분리 결과:
  - 환경 문제 (stale process, port 점유): 해당 없음 — 격리 실행에서도 재현 가능
  - timing 문제: **원인 확인** — 이 테스트는 4회의 파일 요약(stream) + 전체 transition flow (emit → apply → confirm → stop → reverse → conflict check)를 실행하며, mock stream delay 80ms × (응답 길이 / 28 chunk) 누적으로 실행 시간이 약 60초에 달함
  - 기능 regression: 해당 없음 — flow 자체는 정상 동작

## 핵심 변경
- `e2e/tests/web-smoke.spec.mjs` line 568: `testInfo.setTimeout(120_000)` 추가
  - 이 테스트만 개별 timeout을 120초로 확장
  - 전역 timeout (60초)은 변경하지 않음 — 다른 테스트에 영향 없음
  - mock stream delay나 chunk_size를 줄이는 방법도 있으나, 다른 시나리오의 stream 동작 검증에 영향을 줄 수 있어 가장 안전한 최소 수정 선택

## 검증
- 격리 실행: `cd e2e && npx playwright test -g "same-session recurrence aggregate"` — 1 passed (1.0m)
- full suite: `make e2e-test` — 12 passed (4.5m)
- `python3 -m py_compile app/web.py tests/test_web_app.py` — 통과
- `python3 -m unittest -v tests.test_web_app` — 97 tests OK
- `git diff --check` — 통과

## 남은 리스크
- 테스트 실행 시간이 60초로 여전히 경계에 가깝습니다. 시스템 부하가 매우 높은 경우 120초도 부족할 수 있으나, 현실적으로 충분한 여유입니다.
- app code는 변경하지 않았습니다.
- dirty worktree가 여전히 넓으므로 다음 라운드도 unrelated 변경을 건드리지 않아야 합니다.
