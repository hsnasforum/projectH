# Optional template for Codex sessions that should also target a local Ollama-compatible endpoint.
# Verify this against your installed Codex version before making it the default project config.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
project_root_markers = [".git", "README.md", "AGENTS.md"]
developer_instructions = "Follow AGENTS.md. Keep the project local-first, safe, approval-based, and within MVP scope."
model_provider = "ollama"
model = "qwen2.5-coder:7b"

[sandbox_workspace_write]
network_access = false

[model_providers.ollama]
name = "Ollama"
base_url = "http://localhost:11434/v1"
