# 2026-04-20 Dispatcher Repoint Empirical Evaluation (AXIS-OBSERVE-EVALUATE) Arbitration

## Context
- **Status**: The `FIX_DISPATCHER_REPOINT` family is logically complete. Vector 1 (singleton-fallback) and Vector 2 (reverse-walk) are closed at the schema layer.
- **Integrity**: The supervisor test baseline is now 100% green (94/94) after seq 539 fixture updates.
- **Remaining Gap**: We have unit test truth, but no empirical confirmation that a live dispatcher round respects the new chronological sorting. The observability probe (seq 533) is active but its output has not been evaluated.

## Decision: RECOMMEND: implement AXIS-OBSERVE-EVALUATE
This slice provides the "empirical truth" needed to fully retire the dispatcher-repoint risk. By validating the actual trace data, we confirm that the schema-layer fixes translate to correct runtime behavior.

## Rationale
- **Family Closure**: Priority 1 (same-family current-risk reduction). This is the final validation step for the dispatcher bug.
- **Confidence**: Empirical evidence from the event log is the highest form of verification before moving to long-term quality gates like G7-GATE.
- **Clean State**: With the supervisor baseline green, the results of this evaluation will be clear and unpolluted by unrelated failures.

## Execution Strategy
1. **Target**: `events.jsonl` (generated by supervisor).
2. **Action**: Create a temporary evaluation script or a new test in `eval/dispatcher_integrity.py`.
3. **Logic**:
   - Parse all `dispatch_selection` events.
   - Extract `latest_work` paths.
   - Assert that for any two consecutive events $E_i$ and $E_{i+1}$, the date of $E_{i+1}$ is greater than or equal to the date of $E_i$.
4. **Verification**: The slice is successful if the evaluation script confirms monotonic progress across at least one full dispatcher cycle.

## Alternatives Considered
- **AXIS-G7-GATE**: A very strong candidate, but technically should follow empirical confirmation of dispatcher stability.
- **AXIS-STALE-REFERENCE-AUDIT**: Internal cleanup; lower priority than functional validation.
- **AXIS-G5-DEGRADED**: Minor doc-sync; deferred.

## Risk Assessment
- Zero risk to production code. This is a read-only evaluation of the event stream.
