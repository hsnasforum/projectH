# 2026-04-29 M90 번들 발행 및 머지 게이트 해결 제언 (Gemini)

## 1. 개요
M90 Axis 1(UI 상태 연동) 및 Axis 2(dist 재빌드 및 E2E 안정화) 작업이 완료되어 PR #80으로 발행되었습니다. 현재 PR #71부터 #80까지의 대규모 스택이 머지 대기 중인 상태를 해소하고 M91 계획 단계로 넘어가기 위한 권고를 남깁니다.

## 2. 조사 및 판단

### 2.1. 구현 및 발행 상태
- **M90 완료:** `PreferencePanel.tsx`의 상태 연동, `dist` 결과물 동기화, 그리고 Playwright E2E 테스트 안정화가 모두 완료되었습니다 (#80).
- **문서 동기화 완료:** M90 완료 사실을 `MILESTONES.md` 및 `TASK_BACKLOG.md`에 반영하여 bundle commit에 포함했습니다.
- **작업트리:** 현재 `feat/m90-bundle` 브랜치에서 작업트리는 클린하며, 모든 변경사항이 원격 저장소에 반영되어 있습니다.

### 2.2. 정체 원인 분석
- `verify/handoff` 단계에서 다음 제어권이 넘어오지 못한 원인은, 10개에 달하는 누적 PR(#71~#80)의 머지가 `GEMINI.md` 및 `operator_policy`에 따라 오퍼레이터의 직접 승인이 필요한 `merge_gate` 영역이기 때문입니다.
- 로컬에서 수행 가능한 모든 자동화 슬라이스가 소진되었으며, 브랜치 통합은 오퍼레이터의 권한입니다.

### 2.3. 결정 지점
- 정체 해결을 위해서는 누적된 10개의 PR을 순차적으로 머지하고 메인 브랜치를 통합하는 결정이 필요합니다.

## 3. 권고 (Recommendation)

**RECOMMEND: needs_operator authorize_pr_merge_71_to_80**

### 상세 가이드:
1. **머지 실행 승인:** PR #71~#80 전체에 대한 메인 브랜치 머지 및 통합을 승인합니다.
2. **순서 및 리타겟팅:**
   - `#71 → #72 → #73 → #74 → #75` 순차 머지.
   - 이후 `#76`부터 `#80`까지 순차적으로 base를 리타겟팅(retarget)하며 머지하여 스택 구조를 해소합니다.
3. **M91 전환:** 모든 머지 완료 후, 깨끗해진 main 브랜치 기준으로 M91 방향을 결정하는 신규 advisory 라운드로 전환합니다.

---
**STATUS:** advice_ready
**CONTROL_SEQ:** 1364
