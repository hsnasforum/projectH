# 2026-04-29 M88 문서 동기화

## 변경 파일
- `docs/MILESTONES.md`
- `docs/TASK_BACKLOG.md`
- `work/4/29/2026-04-29-m88-doc-sync.md`

## 사용 skill
- `doc-sync`: M88 Axis 1 구현·검증 사실을 milestone/backlog 문서에 맞춰 반영했다.
- `work-log-closeout`: 문서 동기화 라운드의 변경 파일, 실제 검증, 남은 리스크를 한국어 closeout으로 정리했다.

## 변경 이유
- `docs/MILESTONES.md`는 M87 SQLiteSessionStore Global Audit Summary Parity까지만 완료 항목이 기록되어 있어 M88 Axis 1 `list_preferences_payload()` + `get_candidates()` wiring 완료 사실이 누락되어 있었다.
- `docs/TASK_BACKLOG.md` line 9 Remaining 요약도 M87 Axis 1까지만 반영되어 candidate preferences payload wiring 완료 사실을 따라가지 못했다.

## 핵심 변경
- `docs/MILESTONES.md`의 M87 섹션 다음에 `M88 Candidate Preferences Payload Wiring` 완료 섹션을 추가했다.
- M88 Axis 1이 `list_preferences_payload()`에 `candidate_preferences` 키를 추가하고 `get_candidates()` feature-detect + `list_all()` fallback을 사용한다는 현재 구현 사실을 문서화했다.
- 신규 테스트 2건과 `tests.test_preference_handler` 22 tests OK 검증 사실을 반영했다.
- `docs/MILESTONES.md`의 `Next 3 Implementation Priorities`를 PR #71-#77 스택, M88 Axis 1 완료, M89 advisory 결정 예정으로 갱신했다.
- `docs/TASK_BACKLOG.md` line 9 Remaining 문장에 `M88 Axis 1 list_preferences_payload() candidate_preferences wiring (get_candidates())`를 추가했다.

## 검증
- `sha256sum .pipeline/implement_handoff.md` 확인: 요청된 `ca6faa868793051ac24cb6b91e30eb2c4c8a5780c5ce911da030e9aaa550d7be`와 일치.
- `git branch --show-current && git rev-parse --short HEAD` 확인: `feat/m87-bundle`, `bddd1de`.
- `git switch -c feat/m88-doc-sync` 실패: `.git/refs/...lock` 생성이 읽기 전용 파일 시스템으로 차단됨.
- `sed -n '1,180p' verify/4/29/2026-04-29-m88-axis1-get-candidates-wiring.md`로 M88 Axis 1 검증 사실 확인.
- `git diff --check -- docs/MILESTONES.md docs/TASK_BACKLOG.md` 통과.
- `git diff --name-only -- docs/MILESTONES.md docs/TASK_BACKLOG.md` 출력: `docs/MILESTONES.md`, `docs/TASK_BACKLOG.md`.
- `git status --short -- app app/frontend app/static/dist e2e storage` 확인: 기존 M88 Axis 1 `app/handlers/preferences.py` 수정만 표시, frontend/dist/e2e/storage 출력 없음.

## 남은 리스크
- 이번 handoff 범위가 문서 동기화라 코드, 테스트, dist, E2E는 수정하지 않았고 별도 단위 테스트나 Playwright는 실행하지 않았다.
- 기존 작업트리에는 이전 M88 Axis 1 라운드의 `app/handlers/preferences.py`, `tests/test_preference_handler.py` 미커밋 변경이 남아 있다. 이번 라운드에서 새로 수정한 파일은 문서 2개와 이 closeout이다.
- 로컬 브랜치 생성은 `.git/refs` 쓰기 제한으로 수행하지 못했다. commit, push, branch/PR publish는 수행하지 않았다.
