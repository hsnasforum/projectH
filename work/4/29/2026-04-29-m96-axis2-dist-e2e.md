# 2026-04-29 M96 Axis 2 dist 재빌드와 preference E2E 격리

## 변경 파일

- `app/frontend/src/components/MessageBubble.tsx`
  - M96 Axis 1 handoff base 변경을 포함. 이번 Axis 2에서는 React 컴포넌트를 추가 수정하지 않았다.
- `app/static/dist/assets/index.js`
- `work/4/29/2026-04-29-m96-axis2-dist-e2e.md`

## 사용 skill

- `finalize-lite`: 구현 라운드 종료 전 변경 파일, 실행 검증, doc-sync 필요 여부, `/work` closeout 준비 상태를 점검하기 위해 사용.
- `work-log-closeout`: 변경 파일, 실행 검증, 남은 리스크를 `/work` closeout 형식으로 정리하기 위해 사용.

## 변경 이유

- M96 Axis 1에서 추가한 `data-testid="pref-navigate-to-card"`를 production dist에 반영해야 했다.
- preference 관련 E2E 격리 실행으로 기존 선호 패널/팝오버 흐름이 깨지지 않았는지 확인해야 했다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 production dist를 재빌드했다.
- 빌드 결과 `app/static/dist/assets/index.js`가 갱신되었다.
- dist에서 `pref-navigate-to-card` 문자열이 포함된 것을 확인했다.
- `preference` E2E 그룹 5건이 모두 통과해 selector/fixture 수정은 하지 않았다.
- handoff 경계에 따라 추가 React 컴포넌트 수정, E2E 파일 수정, docs 변경, `.pipeline` control 변경, 커밋/push/PR 작업은 하지 않았다.

## 검증

- `git switch -c feat/m96-axis2-dist-e2e`
  - 실패: `.git/refs/heads/feat/m96-axis2-dist-e2e.lock` 생성이 `Read-only file system`으로 거부되어 브랜치를 만들 수 없었다.
- `cd app/frontend && npx vite build`
  - 통과. `../static/dist/assets/index.js   318.39 kB │ gzip: 95.33 kB`, `✓ built in 2.04s`.
- `cd e2e && npx playwright test tests/web-smoke.spec.mjs -g "preference" --reporter=line`
  - 통과. `5 passed (32.2s)`.
- `ls -lh app/static/dist/assets/index.js`
  - 확인: `314K Apr 29 17:51 app/static/dist/assets/index.js`.
- `grep -c "pref-navigate-to-card" app/static/dist/assets/index.js`
  - 통과. 출력 `1`.
- `git diff --check -- app/static/dist/assets/index.js`
  - 통과. 출력 없음.
- `git diff --check -- app/static/dist`
  - 통과. 출력 없음.
- `git status --short -- app/frontend/src/components/MessageBubble.tsx app/static/dist e2e`
  - 확인: `MessageBubble.tsx`, `app/static/dist/assets/index.js` 수정 상태. `e2e/` 파일 변경 없음.
- `git diff --stat -- app/frontend/src/components/MessageBubble.tsx app/static/dist e2e`
  - 확인: 2개 파일, 2 insertions, 1 deletion.

## 남은 리스크

- 전체 Playwright smoke, `make e2e-test`, 전체 unit test는 실행하지 않았다. 이번 handoff가 dist 재빌드와 `preference` E2E 격리 실행으로 한정되어 좁은 검증만 수행했다.
- 이번 빌드에서는 `app/static/dist/assets/index.css` 변경은 없고 `index.js`만 갱신되었다.
- 로컬 `.git/refs` 쓰기 제한 때문에 요청된 브랜치 생성은 실패했다. 커밋, push, PR 생성은 수행하지 않았다.
