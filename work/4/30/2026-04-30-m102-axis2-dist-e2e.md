# 2026-04-30 M102 Axis 2 dist/E2E

## 변경 파일

- `app/static/dist/assets/index.js` — M102 preference delete frontend 변경 반영 dist 재빌드
- `app/static/dist/assets/index.css` — Vite/Tailwind 재빌드 산출물 갱신
- `e2e/tests/web-smoke.spec.mjs` — `preference delete removes preference from list` smoke 시나리오 추가
- `work/4/30/2026-04-30-m102-axis2-dist-e2e.md` — 이번 closeout

## 사용 skill

- `e2e-smoke-triage` — PreferencePanel delete selector/flow에 맞는 최소 Playwright smoke 추가 범위를 확인하기 위해 사용
- `work-log-closeout` — `/work` closeout 필수 섹션, 실제 검증 기록, 남은 리스크 형식을 맞추기 위해 사용

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1456이 M102 Axis 1 preference delete 구현 이후 dist 재빌드와 E2E 시나리오 추가를 지시했다.
- Axis 1에서 추가된 `data-testid="delete-preference-btn"`가 shipped dist에 반영됐는지 확인하고, PreferencePanel 목록에서 삭제 후 카드가 사라지는 browser contract를 smoke로 고정했다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 `app/static/dist/assets/index.js`와 `app/static/dist/assets/index.css`를 재빌드했다.
- dist JS에 `delete-preference-btn` 문자열이 1건 포함됨을 확인했다.
- `e2e/tests/web-smoke.spec.mjs`에 mock preference payload와 `DELETE /api/preferences/<id>` route를 사용하는 삭제 smoke를 추가했다.
- 새 smoke는 기존 preference 카드 표시 → `delete-preference-btn` 클릭 → 카드 hidden → DELETE 요청 1회를 확인한다.
- `app/frontend/src/` 소스 파일은 이번 Axis 2에서 추가 편집하지 않았다.

## 검증

- `sha256sum .pipeline/implement_handoff.md` — PASS, `5143a969d99c9a957207132eb168afbba17a56e2b81acf76fa9ee2b887dcf8cf`
- `cd app/frontend && npx vite build` — PASS
- `grep -c "delete-preference-btn" app/static/dist/assets/index.js` — PASS, `1`
- `git diff --check -- app/static/dist/assets/index.js e2e/tests/web-smoke.spec.mjs` — PASS
- `git diff --check -- app/frontend/src/` — PASS
- `git diff --check -- app/static/dist/assets/index.js app/static/dist/assets/index.css e2e/tests/web-smoke.spec.mjs app/frontend/src/` — PASS
- Playwright scenario local run — 미실행, handoff가 현재 sandbox socket 제한으로 CI 위임을 명시

## 남은 리스크

- 새 Playwright 시나리오의 실제 브라우저 실행은 CI에 위임됐다.
- `app/frontend/src/api/client.ts`, `app/frontend/src/components/PreferencePanel.tsx`의 Axis 1 dirty 변경은 이번 handoff 범위 밖이라 수정하지 않았다.
- product docs 동기화는 이번 Axis 2 handoff 범위가 아니어서 수행하지 않았다.
- commit, push, PR publish는 implement role 제약에 따라 수행하지 않았다.
