# 2026-04-30 M102 doc-sync

## 변경 파일

- `docs/MILESTONES.md` - M102 preference delete Axis 1/Axis 2 완료 사실과 현재 doc-sync 상태 반영
- `docs/TASK_BACKLOG.md` - reviewed-memory next phase target에 M102 preference delete 범위 추가
- `docs/ACCEPTANCE_CRITERIA.md` - PreferencePanel 선호 삭제 acceptance 항목 추가
- `docs/PRODUCT_SPEC.md` - PreferencePanel 선호 삭제 shipped behavior 추가
- `work/4/30/2026-04-30-m102-doc-sync.md` - 이번 closeout

## 사용 skill

- `doc-sync` - 구현/검증 사실과 product docs의 현재 동작 표현을 맞추기 위해 사용
- `work-log-closeout` - 구현 라운드 종료 기록 형식과 실제 검증/잔여 리스크 기록을 맞추기 위해 사용

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1457이 M102 preference delete 이후 `MILESTONES`, `TASK_BACKLOG`, `ACCEPTANCE_CRITERIA`, `PRODUCT_SPEC` 문서 동기화를 지시했다.
- 최신 verify 기록 `verify/4/30/2026-04-30-m102-axis2-dist-e2e.md` 기준으로 Axis 1 코드, Axis 2 dist rebuild, E2E smoke 추가는 완료됐고 product docs만 미동기 상태였다.

## 핵심 변경

- `docs/MILESTONES.md`에 M102 preference delete 섹션을 추가해 `DELETE /api/preferences/<preference_id>`, `deletePreference()`, `delete-preference-btn`, dist rebuild, E2E smoke 추가를 기록했다.
- `docs/TASK_BACKLOG.md`의 next phase target에 M102 preference delete 완료 범위를 추가했다.
- `docs/ACCEPTANCE_CRITERIA.md`에 PreferencePanel 삭제 버튼이 `DELETE /api/preferences/<preference_id>`를 호출하고 목록에서 제거되는 acceptance 항목을 추가했다.
- `docs/PRODUCT_SPEC.md`에 `deletePreference()` 기반 명시적 선호 삭제 동작과 ACTIVE 선호 비활성화 후 제거 동작을 현재 shipped behavior로 반영했다.
- handoff 제약에 따라 코드, dist, E2E 파일은 이번 라운드에서 추가 편집하지 않았다.

## 검증

- `sha256sum .pipeline/implement_handoff.md` - PASS, `bf368615213b094a1fcbaf188ff1db64f236865601a50d4f08453ae3e0a83769`
- `git diff --check -- docs/MILESTONES.md docs/TASK_BACKLOG.md docs/ACCEPTANCE_CRITERIA.md docs/PRODUCT_SPEC.md` - PASS
- `rg -n "delete-preference-btn|preference delete|deletePreference" docs/MILESTONES.md docs/ACCEPTANCE_CRITERIA.md docs/PRODUCT_SPEC.md` - PASS
- `rg -n "M102 preference delete|M102 doc-sync" docs/MILESTONES.md docs/TASK_BACKLOG.md` - PASS

## 남은 리스크

- Playwright scenario의 실제 브라우저 실행은 이전 Axis 2 closeout과 verify 기록처럼 CI에 위임된 상태다.
- 이번 라운드는 docs-only handoff라 기존 M102 코드, dist, E2E dirty 변경은 수정하지 않았다.
- commit, push, branch/PR publish는 implement role 제약에 따라 수행하지 않았다.
