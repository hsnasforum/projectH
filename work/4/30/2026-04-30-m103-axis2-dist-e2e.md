# 2026-04-30 M103 Axis 2 dist/E2E

## 변경 파일

- `app/static/dist/assets/index.js` - M103 preference reliability toggle frontend 변경 반영 dist 재빌드
- `app/static/dist/assets/index.css` - Vite/Tailwind 재빌드 산출물 갱신
- `e2e/tests/web-smoke.spec.mjs` - `preference reliability toggle updates badge` smoke 시나리오 추가
- `work/4/30/2026-04-30-m103-axis2-dist-e2e.md` - 이번 closeout

## 사용 skill

- `e2e-smoke-triage` - PreferencePanel toggle selector/flow의 최소 browser smoke 범위를 확인하기 위해 사용
- `finalize-lite` - 구현 종료 전 실행 사실, 미실행 범위, 문서 동기화 필요 여부를 점검하기 위해 사용
- `work-log-closeout` - `/work` closeout 필수 섹션과 실제 검증/잔여 리스크 기록 형식을 맞추기 위해 사용

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1461이 M103 Axis 1 preference reliability toggle 이후 dist 재빌드와 E2E 시나리오 추가를 지시했다.
- 최신 verify 기록 `verify/4/30/2026-04-30-m103-preference-reliability-toggle.md` 기준으로 Axis 1 backend/frontend/test는 verified됐고, dist 반영과 browser smoke coverage가 남아 있었다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 `app/static/dist/assets/index.js`와 `app/static/dist/assets/index.css`를 재빌드했다.
- dist JS에 `toggle-reliability-btn` 문자열이 1건 포함됨을 확인했다.
- `e2e/tests/web-smoke.spec.mjs`에 `preference reliability toggle updates badge` 시나리오를 추가했다.
- 새 smoke는 mocked preference payload에서 `toggle-reliability-btn` 클릭 → `POST /api/preferences/<id>/toggle-reliability` 1회 → `신뢰도 높음` 배지 숨김 및 버튼 텍스트 `신뢰 설정` 변경을 확인한다.
- handoff 제약에 따라 `app/frontend/src/` 소스 파일은 이번 Axis 2에서 추가 편집하지 않았다.

## 검증

- `sha256sum .pipeline/implement_handoff.md` - PASS, `7317579c2152db67e1f0de7621a2b4e6c11f9f775a7ccb11dd797d04ba6089f5`
- `cd app/frontend && npx vite build` - PASS
- `grep -c "toggle-reliability-btn" app/static/dist/assets/index.js` - PASS, `1`
- `git diff --check -- app/static/dist/assets/index.js e2e/tests/web-smoke.spec.mjs` - PASS
- `git diff --check -- app/frontend/src/` - PASS
- `git diff --check -- app/static/dist/assets/index.js app/static/dist/assets/index.css e2e/tests/web-smoke.spec.mjs` - PASS
- `rg -n "preference reliability toggle updates badge|toggle-reliability-btn|toggle-reliability" e2e/tests/web-smoke.spec.mjs app/static/dist/assets/index.js` - PASS

## 남은 리스크

- Playwright scenario의 실제 브라우저 실행은 handoff가 명시한 현재 sandbox socket 제한 때문에 로컬에서 실행하지 않았고 CI에 위임한다.
- `app/frontend/src/api/client.ts`, `app/frontend/src/components/PreferencePanel.tsx`의 Axis 1 dirty 변경은 이번 handoff 범위 밖이라 수정하지 않았다.
- product docs 동기화는 이번 Axis 2 handoff 범위가 아니어서 수행하지 않았다.
- commit, push, branch/PR publish는 implement role 제약에 따라 수행하지 않았다.
