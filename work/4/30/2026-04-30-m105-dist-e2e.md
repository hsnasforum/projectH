# 2026-04-30 M105 correction history status filter dist/e2e

## 변경 파일

- `app/static/dist/assets/index.js`
- `app/static/dist/assets/index.css`
- `e2e/tests/web-smoke.spec.mjs`
- `work/4/30/2026-04-30-m105-dist-e2e.md`

## 사용 skill

- `e2e-smoke-triage`: 기존 Playwright smoke의 교정 이력 route stub, selector, 목록 검증 패턴을 재사용해 시나리오 추가 범위를 좁혔다.
- `finalize-lite`: handoff 검증 범위와 미실행 Playwright 리스크를 closeout 전에 정리했다.
- `work-log-closeout`: 변경 파일, 실제 명령, 잔여 리스크를 `/work` 형식으로 기록했다.

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1469 / `m105_axis2_dist_e2e` 지시에 따라 M105 Axis 1의 correction history status filter UI를 정적 번들에 반영하고 E2E smoke 시나리오를 추가했다.
- docs sync는 handoff 지시대로 이후 operator_retriage 인라인 번들 범위로 남겼다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 `app/static/dist/`를 재빌드했다.
- dist `index.js`에 `correction-status-filter` test id가 포함되도록 반영했다.
- `e2e/tests/web-smoke.spec.mjs`에 `correction history status filter narrows list` 시나리오를 추가했다.
- 새 시나리오는 `correction-status-filter`에서 `confirmed`를 선택한 뒤 `GET /api/corrections/list?status=confirmed` 요청이 1회 발생하고 confirmed 목록만 표시되는지 검증한다.
- `app/frontend/src/`는 이번 Axis 2에서 추가 편집하지 않았다.

## 검증

- `cd app/frontend && npx vite build` 통과.
- `grep -c "correction-status-filter" app/static/dist/assets/index.js` → `1`.
- `git diff --check -- app/static/dist/assets/index.js e2e/tests/web-smoke.spec.mjs` 통과.
- `git diff --check -- app/frontend/src/` 통과.
- `rg -n "correction history status filter narrows list|correction-status-filter|status=confirmed" e2e/tests/web-smoke.spec.mjs app/static/dist/assets/index.js`로 시나리오와 dist 반영 확인.

## 남은 리스크

- handoff 지시에 따라 Playwright webServer 실행은 하지 않았다. sandbox socket 제한으로 E2E 실제 실행은 CI 위임이다.
- `app/frontend/src/components/PreferencePanel.tsx`는 Axis 1 변경분으로 이미 dirty 상태였으며, 이번 Axis 2에서 추가 편집하지 않았다.
- commit, push, PR publish는 implement lane 금지사항에 따라 수행하지 않았다.
