# 2026-04-30 M107 correction history pagination dist/e2e

## 변경 파일

- `app/static/dist/assets/index.js`
- `e2e/tests/web-smoke.spec.mjs`
- `work/4/30/2026-04-30-m107-dist-e2e.md`

## 사용 skill

- `e2e-smoke-triage`: 기존 correction history smoke 패턴을 재사용해 show-more offset append 시나리오를 추가했다.
- `finalize-lite`: dist 반영, E2E 추가, 미실행 Playwright 리스크를 closeout 전에 점검했다.
- `work-log-closeout`: 실제 변경 파일과 실행한 검증만 `/work` 형식으로 기록했다.

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1478 / `m107_axis2_dist_e2e` 지시에 따라 M107 Axis 1의 offset pagination 변경을 정적 번들에 반영하고 E2E smoke 시나리오를 추가했다.
- docs sync는 handoff 지시대로 별도 implement 라운드 없이 이후 operator_retriage 인라인 번들 범위로 남겼다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 frontend dist를 재빌드했다.
- dist `index.js`에 `offset` 관련 correction list 호출 코드가 포함됨을 확인했다.
- `e2e/tests/web-smoke.spec.mjs`에 `correction history show more appends next page` 시나리오를 추가했다.
- 새 시나리오는 `correction-show-more-btn` 클릭 후 `GET /api/corrections/list?offset=N&limit=N` 요청 발생과 기존 목록에 다음 페이지 항목이 append되는지 검증한다.
- `app/frontend/src/` 소스 파일은 이번 Axis 2에서 추가 편집하지 않았다.

## 검증

- `cd app/frontend && npx vite build` 통과.
- `grep -c "correctionListHasMore\\|offset" app/static/dist/assets/index.js` → `3`.
- `git diff --check -- app/static/dist/assets/index.js e2e/tests/web-smoke.spec.mjs` 통과.
- `git diff --check -- app/frontend/src/` 통과.
- `rg -n "correction history show more appends next page|offset|correction-show-more-btn" e2e/tests/web-smoke.spec.mjs app/static/dist/assets/index.js`로 시나리오와 dist 반영 확인.

## 남은 리스크

- handoff 지시에 따라 Playwright webServer 실행은 하지 않았다. sandbox socket 제한으로 실제 E2E 실행은 CI 위임이다.
- `app/frontend/src/api/client.ts`, `app/frontend/src/components/PreferencePanel.tsx` 등 M107 Axis 1 변경 파일은 이미 dirty 상태였으며, 이번 Axis 2에서 추가 편집하지 않았다.
- commit, push, PR publish는 implement lane 금지사항에 따라 수행하지 않았다.
