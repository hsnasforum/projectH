# 2026-04-30 M108 dist 재빌드와 preference search E2E

## 변경 파일

- `app/static/dist/assets/index.js`
- `e2e/tests/web-smoke.spec.mjs`
- `work/4/30/2026-04-30-m108-dist-e2e.md`

## 사용 skill

- `e2e-smoke-triage`: 새 UI selector와 PreferencePanel DOM 필터 시나리오의 최소 검증 범위 확인
- `finalize-lite`: 실행한 검증과 미실행 검증을 분리해 구현 라운드 종료 조건 확인
- `work-log-closeout`: 변경 파일, 실행 명령, 잔여 리스크를 `/work` 형식으로 기록

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1482의 `m108_axis2_dist_e2e` 실행.
- Axis 1에서 추가된 `preference-search-input` 클라이언트 필터를 정적 번들에 반영하고, smoke suite에 검색 DOM 동작을 고정해야 했다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 `app/static/dist/assets/index.js`를 재빌드했다.
- `e2e/tests/web-smoke.spec.mjs`에 `preference search filters preference list` 시나리오를 추가했다.
- 새 시나리오는 `/api/preferences`, `/api/preferences/audit`, `/api/corrections/summary`, `/api/corrections/list`를 mock한 뒤 `/app-preview`에서 PreferencePanel만 확인한다.
- 검색어가 `description`에 매칭되는 카드와 `corrected_text`에만 매칭되는 카드를 각각 확인하고, 미매칭 검색어에서는 `검색 결과가 없습니다`를 확인한다.
- 검색어 입력 후 `/api/preferences` 재호출 없이 DOM만 바뀌는지도 `preferenceRequests` 카운트로 확인하도록 했다.
- `app/frontend/src/` 소스는 이번 Axis 2에서 추가 편집하지 않았다.

## 검증

- `cd app/frontend && npx vite build` 통과.
- `grep -c "preference-search-input" app/static/dist/assets/index.js` 결과: `1`.
- `git diff --check -- app/static/dist/assets/index.js e2e/tests/web-smoke.spec.mjs` 통과.
- `git diff --check -- app/frontend/src/` 통과.
- `node --check e2e/tests/web-smoke.spec.mjs` 통과.
- `grep -n "preference search filters preference list" e2e/tests/web-smoke.spec.mjs` 확인: `14556:test("preference search filters preference list", async ({ page }) => {`.

## 남은 리스크

- handoff에 명시된 sandbox socket 제한 때문에 Playwright webServer 실행은 하지 않았고 CI에 위임했다.
- `app/frontend/src/components/PreferencePanel.tsx`는 Axis 1 선행 변경으로 dirty 상태에 남아 있으나, 이번 Axis 2에서는 추가 편집하지 않았다.
- docs sync는 handoff의 3+ docs-only 규칙에 따라 이후 operator_retriage 인라인 번들로 남겼다.
- commit, push, branch/PR publish, next-slice 선택은 수행하지 않았다.
