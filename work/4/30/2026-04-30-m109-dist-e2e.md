# 2026-04-30 M109 dist 재빌드와 preference pagination E2E

## 변경 파일

- `app/static/dist/assets/index.js`
- `e2e/tests/web-smoke.spec.mjs`
- `work/4/30/2026-04-30-m109-dist-e2e.md`

## 사용 skill

- `e2e-smoke-triage`: `preference-show-more-btn` selector와 preference pagination 브라우저 흐름의 최소 검증 범위 확인
- `finalize-lite`: 실행한 검증과 미실행 검증, doc-sync 잔여 범위 확인
- `work-log-closeout`: 변경 파일, 실행 명령, 잔여 리스크를 `/work` 형식으로 기록

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1486의 `m109_axis2_dist_e2e` 실행.
- Axis 1에서 추가된 preference list pagination을 정적 번들에 반영하고, M107 correction pagination과 대칭인 show-more append 시나리오를 smoke suite에 고정해야 했다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 `app/static/dist/assets/index.js`를 재빌드했다.
- `e2e/tests/web-smoke.spec.mjs`에 `preference show more appends next page` 시나리오를 추가했다.
- 새 시나리오는 `/api/preferences?limit=20` 초기 batch 20개와 `offset>0` 다음 batch 1개를 mock하고, `preference-show-more-btn` 클릭 후 카드가 21개로 append되는지 확인한다.
- 같은 E2E 파일의 기존 `/api/preferences` mock route들을 query 허용 패턴으로 보정해 `fetchPreferences({ limit })` 호출을 계속 가로채도록 했다.
- `app/frontend/src/` 소스 파일은 이번 Axis 2에서 추가 편집하지 않았다.

## 검증

- `cd app/frontend && npx vite build` 통과.
- `grep -cE "preference-show-more-btn|preferenceListHasMore" app/static/dist/assets/index.js` 결과: `1`.
- `git diff --check -- app/static/dist/assets/index.js e2e/tests/web-smoke.spec.mjs` 통과.
- `git diff --check -- app/frontend/src/` 통과.
- `node --check e2e/tests/web-smoke.spec.mjs` 통과.
- `grep -n "preference show more appends next page" e2e/tests/web-smoke.spec.mjs` 확인: `14556:test("preference show more appends next page", async ({ page }) => {`.

## 남은 리스크

- handoff에 명시된 sandbox socket 제한 때문에 Playwright webServer 실행은 하지 않았고 CI에 위임했다.
- `app/frontend/src/api/client.ts`와 `app/frontend/src/components/PreferencePanel.tsx`는 Axis 1 선행 변경으로 dirty 상태에 남아 있으나, 이번 Axis 2에서는 추가 편집하지 않았다.
- docs sync는 handoff의 3+ docs-only 규칙에 따라 이후 operator_retriage 인라인 번들로 남겼다.
- commit, push, branch/PR publish, next-slice 선택은 수행하지 않았다.
