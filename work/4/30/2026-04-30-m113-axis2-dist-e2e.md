# 2026-04-30 M113 Axis 2 dist 재빌드와 E2E smoke

## 변경 파일

- `app/static/dist/assets/index.js`
- `e2e/tests/web-smoke.spec.mjs`
- `work/4/30/2026-04-30-m113-axis2-dist-e2e.md`

## 사용 skill

- `e2e-smoke-triage`: Review Queue 브라우저 smoke 추가 범위와 isolated rerun 기준을 확인했습니다.
- `work-log-closeout`: 구현 라운드 종료 기록 형식과 실제 검증 결과 기록 기준을 확인했습니다.

## 변경 이유

- `.pipeline/implement_handoff.md` CONTROL_SEQ 1506 지시에 따라 M113 Axis 1의 Review Queue 액션 인라인 이동을 정적 번들에 반영하고, 액션 버튼이 context 섹션보다 먼저 렌더링되는지 smoke로 고정했습니다.

## 핵심 변경

- `cd app/frontend && npx vite build`로 `app/static/dist/assets/index.js`를 재빌드했습니다.
- `e2e/tests/web-smoke.spec.mjs`에 `review queue action buttons appear before context section` 시나리오를 추가했습니다.
- 새 smoke는 fake `review_queue_items` 1개와 `context_turns`, `evidence_summary`를 로드한 뒤 `review-accept`, `review-defer`, `review-reject`, `review-context-turns` 표시를 확인합니다.
- `compareDocumentPosition`으로 `review-accept`가 `review-context-turns`보다 DOM상 앞서는지 검증합니다.
- handoff 제약에 따라 `app/frontend/src/` 소스는 이번 Axis 2에서 추가 편집하지 않았습니다.

## 검증

- `cd app/frontend && npx vite build` — PASS
- `grep -c "review-accept" app/static/dist/assets/index.js` — PASS (`1`)
- `grep -c "review-context-turns" app/static/dist/assets/index.js` — PASS (`1`)
- `git diff --check -- app/static/dist/assets/index.js e2e/tests/web-smoke.spec.mjs` — PASS
- `git diff --check -- app/frontend/src/` — PASS
- `cd e2e && npx playwright test tests/web-smoke.spec.mjs -g "review queue action buttons appear before context section" --reporter=line` — PASS (`1 passed`)

## 남은 리스크

- 전체 Playwright suite와 `make e2e-test`는 실행하지 않았습니다. 이번 변경은 단일 Review Queue smoke 추가와 dist 반영에 한정되어 isolated scenario만 실행했습니다.
- `app/frontend/src/components/ReviewQueuePanel.tsx`의 dirty diff는 M113 Axis 1 결과이며, 이번 Axis 2에서는 추가 편집하지 않았습니다.
