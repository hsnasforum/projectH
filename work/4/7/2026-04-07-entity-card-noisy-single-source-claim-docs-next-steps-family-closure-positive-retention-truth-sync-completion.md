# entity-card noisy single-source claim docs-next-steps family-closure positive-retention truth-sync completion

## 변경 파일
- `docs/NEXT_STEPS.md`

## 사용 skill
- 없음 (handoff 기반 직접 구현)

## 변경 이유
- `docs/NEXT_STEPS.md:16`의 noisy single-source claim family summary가 `백과 기반`, `namu.wiki`, `ko.wikipedia.org` positive retention을 빠뜨림.

## 핵심 변경
- follow-up/second-follow-up chain provenance truth-sync wording에 `백과 기반`, `namu.wiki`, `ko.wikipedia.org` positive retention 명시

## 검증
- `git diff --check`: clean

## 남은 리스크
- scenario count 73 유지.
